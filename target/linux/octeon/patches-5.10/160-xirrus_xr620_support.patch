--- a/arch/mips/cavium-octeon/executive/cvmx-helper-board.c
+++ b/arch/mips/cavium-octeon/executive/cvmx-helper-board.c
@@ -177,6 +177,13 @@ int cvmx_helper_board_get_mii_address(in
 			return -1;
 	case CVMX_BOARD_TYPE_UBNT_E200:
 		return -1;
+	case CVMX_BOARD_TYPE_XIRRUS_XR620:
+		if (ipd_port == 0)
+			return 1;
+		else if (ipd_port == 16)
+			return 16 + 2;
+		else
+		    return -1;
 	case CVMX_BOARD_TYPE_KONTRON_S1901:
 		if (ipd_port == CVMX_HELPER_BOARD_MGMT_IPD_PORT)
 			return 1;
@@ -319,6 +326,12 @@ int __cvmx_helper_board_interface_probe(
 		if (interface == 1)
 			return 0;
 		break;
+	case CVMX_BOARD_TYPE_XIRRUS_XR620:
+		if (interface == 0)
+			return 1;
+		if (interface == 1)
+			return 1;
+		break;
 	}
 	return supported_ports;
 }
--- a/arch/mips/include/asm/octeon/cvmx-bootinfo.h
+++ b/arch/mips/include/asm/octeon/cvmx-bootinfo.h
@@ -300,6 +300,7 @@ enum cvmx_board_types_enum {
 	CVMX_BOARD_TYPE_UBNT_E220 = 20005,
 	CVMX_BOARD_TYPE_ITUS_SHIELD = 20006,
 	CVMX_BOARD_TYPE_UBNT_E300 = 20300,
+	CVMX_BOARD_TYPE_XIRRUS_XR620 = 20495,
 	CVMX_BOARD_TYPE_KONTRON_S1901 = 21901,
 	CVMX_BOARD_TYPE_CUST_PRIVATE_MAX = 30000,
 
@@ -405,6 +406,7 @@ static inline const char *cvmx_board_typ
 		ENUM_BRD_TYPE_CASE(CVMX_BOARD_TYPE_UBNT_E220)
 		ENUM_BRD_TYPE_CASE(CVMX_BOARD_TYPE_ITUS_SHIELD)
 		ENUM_BRD_TYPE_CASE(CVMX_BOARD_TYPE_UBNT_E300)
+		ENUM_BRD_TYPE_CASE(CVMX_BOARD_TYPE_XIRRUS_XR620)
 		ENUM_BRD_TYPE_CASE(CVMX_BOARD_TYPE_KONTRON_S1901)
 		ENUM_BRD_TYPE_CASE(CVMX_BOARD_TYPE_CUST_PRIVATE_MAX)
 	}
